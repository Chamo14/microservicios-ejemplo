server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: multiple-instances
          uri: https://github.com/Chamo14/microservicios-ejemplo
          search-paths: config-data
  application:
    name: config-service
